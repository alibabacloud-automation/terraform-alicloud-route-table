# Basic Route Table Example

This example demonstrates how to create a basic route table and associate it with an existing VSwitch.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

To clean up resources:

```bash
terraform destroy
```
